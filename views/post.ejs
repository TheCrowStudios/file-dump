<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File dump</title>
    <link rel="stylesheet" href="style.css">
    <link rel="icon" href="/directory_open_file_mydocs-4.png">
</head>
<body>
    <h1>Create a post</h1>

    <div>
        File limits: 4GB
    </div>
    
    <% if (canPost) { %>
        <form enctype="multipart/form-data" action="/" method="POST">
            <div>
                <input name="file" id="file" type="file" multiple>
            </div>

            <div>
                <label for="title">Title</label>
                <input required type="text" name="title" id="title", maxlength="100">
            </div>

            <%#<div>
                <label for="description">Description</label>
                <div>
                    <textarea name="description" id="description" cols="30" rows="10" maxlength="1024"></textarea>
                </div>
            </div>%>
            
            <%= error %>

            <div>
                <button type="submit">Post</button>
            </div>
        </form>
    <% } else { %>
        <%= error %>
    <% } %>
</body>
</html>